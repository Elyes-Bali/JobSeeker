{#{% extends 'base.html.twig' %}#}

{#{% block title %}Nouvelle Réclamation{% endblock %}#}

{#{% block body %}#}
{#    <div class="container mt-5 h-100">#}
{#        <h1 class="mb-4 text-center">Déposer une Nouvelle Réclamation</h1>#}

{#        {{ form_start(form) }}#}

{#        <div class="mb-3">#}
{#            {{ form_label(form.titre, 'Titre de la réclamation', { 'label_attr': { 'class': 'form-label' } }) }}#}
{#            {{ form_widget(form.titre, { 'attr': { 'class': 'form-control', 'placeholder': 'Entrez un titre' } }) }}#}
{#            {{ form_errors(form.titre) }}#}
{#        </div>#}

{#        <div class="mb-3">#}
{#            {{ form_label(form.categorie, 'Type de reclamation', { 'label_attr': { 'class': 'form-label' } }) }}#}
{#            {{ form_widget(form.categorie, { 'attr': { 'class': 'form-control' } }) }}#}
{#            {{ form_errors(form.categorie) }}#}
{#        </div>#}

{#        <div class="mb-3">#}
{#            {{ form_label(form.priorite, 'Niveau de priorite', { 'label_attr': { 'class': 'form-label' } }) }}#}
{#            {{ form_widget(form.priorite, { 'attr': { 'class': 'form-control' } }) }}#}
{#            {{ form_errors(form.priorite) }}#}
{#        </div>#}

{#        <div class="mb-3">#}
{#            {{ form_label(form.description, 'Description détaillée', { 'label_attr': { 'class': 'form-label' } }) }}#}
{#            {{ form_widget(form.description, { 'attr': { 'class': 'form-control', 'placeholder': 'Décrivez votre problème...' } }) }}#}
{#            {{ form_errors(form.description) }}#}
{#        </div>#}

{#        <button type="submit" class="btn btn-primary w-100">Envoyer la Réclamation</button>#}
{#        <a href="{{ path('app_reclamation_list') }}" class="btn btn-primary w-100 mt-2">Annuler</a>#}

{#        {{ form_end(form) }}#}
{#    </div>#}

{#{% endblock %}#}
{% extends 'base.html.twig' %}

{% block title %}Ajouter une Réclamation{% endblock %}

{% block body %}
    <div class="container mt-4 h-100">
        <div class="card shadow-sm">
            <div class="card-header bg-success text-white">
                <h2>Ajouter une Nouvelle Réclamation</h2>
            </div>
            <div class="card-body">
                {{ form_start(form) }}
                <div class="row">
                    <div class="col-md-10 mb-3">
                        <label for="titre" class="form-label"><strong>Titre de la Réclamation</strong></label>
                        {{ form_widget(form.titre, {'attr': {'class': 'form-control', 'placeholder': 'Exemple : Problème de réservation'}}) }}
                        <div class="form-text">Veuillez fournir un titre clair et concis.</div>
                    </div>

                    <div class="col-md-10 mb-3">
                        <label for="categorie" class="form-label"><strong>Catégorie</strong></label>
                        {{ form_widget(form.categorie, {'attr': {'class': 'form-select'}}) }}
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-10 mb-3">
                        <label for="priorite" class="form-label"><strong>Priorité</strong></label>
                        {{ form_widget(form.priorite, {'attr': {'class': 'form-select'}}) }}
                        <div class="form-text">Choisissez la priorité selon l'urgence du problème.</div>
                    </div>

                </div>

                <div class="mb-3">
                    <label for="description" class="form-label"><strong>Description</strong></label>
                    {{ form_widget(form.description, {'attr': {'class': 'form-control', 'rows': '5', 'placeholder': 'Décrivez votre problème en détail...'}}) }}
                </div>

                <div class="text-end">
                    <button type="submit" class="btn btn-success me-2">
                        <i class="bi bi-check-circle"></i> Soumettre
                    </button>
                    <a href="{{ path('app_reclamation_list') }}" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Annuler
                    </a>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
